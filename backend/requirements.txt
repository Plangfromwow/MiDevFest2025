# FastAPI and ASGI server
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Request handling
httpx==0.25.2
requests==2.31.0

# Data validation and settings
pydantic==2.5.0
pydantic-settings==2.0.3
python-dotenv==1.0.0

# Google APIs
google-auth==2.23.4
google-auth-oauthlib==1.1.0
google-api-python-client==2.108.0

# Convex (Python client)
# Note: Install via: pip install convex
# If convex package is not available, remove the import and use HTTP client only
convex==0.5.0

# Logging and monitoring
structlog==23.2.0

# Development dependencies (optional)
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
isort==5.12.0
mypy==1.7.1

# Security
cryptography==41.0.7